{
  "expo": {
    "name": "TenantPro",
    "slug": "mobile",
    "owner": "anush_kulal",
    "version": "1.3.0",
    "orientation": "portrait",
    "icon": "./assets/icon-tp.png",
    "userInterfaceStyle": "automatic",
    "runtimeVersion": { "policy": "fingerprint" },
    "updates": {
      "enabled": true,
      "url": "https://u.expo.dev/3b258101-bef2-412f-8be0-53af8b224dfb",
      "checkAutomatically": "ON_ERROR_RECOVERY",
      "fallbackToCacheTimeout": 0
    },
    "splash": {
      "image": "./assets/splash-tp.png",
      "resizeMode": "contain",
      "backgroundColor": "#08080A"
    },
    "ios": {
      "supportsTablet": true
    },
    "android": {
      "package": "com.anushkulal.tenantpro",
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-tp-foreground.png",
        "backgroundColor": "#C8F751"
      }
    },
    "web": {
      "favicon": "./assets/favicon-tp.png"
    },
    "plugins": [
      "@react-native-community/datetimepicker",
      [
        "expo-camera",
        {
          "cameraPermission": "TenantPro uses the camera to scan a property's invite QR code."
        }
      ],
      [
        "expo-location",
        {
          "locationAlwaysAndWhenInUsePermission": "TenantPro uses your location only while you are placing a property on the map, so the pin can start where you are standing.",
          "locationWhenInUsePermission": "TenantPro uses your location only while you are placing a property on the map, so the pin can start where you are standing.",
          "isAndroidBackgroundLocationEnabled": false
        }
      ]
    ],
    "extra": {
      "eas": {
        "projectId": "3b258101-bef2-412f-8be0-53af8b224dfb"
      },
      "whatsNew": "Add glass design system: theme tokens, UI kit, navigation flow helpers\nFix OTA pointing app at dead LAN IP — use hosted backend\nFail fast when DB is down + add back button to role login screens\nFix login instability and add guest login + auto-OTA\nShow a 'What's new' changelog in the in-app update popup\nTenant portal Phase 1: role selection + tenant accounts",
      "releaseNotes": [
        "Add glass design system: theme tokens, UI kit, navigation flow helpers",
        "Fix OTA pointing app at dead LAN IP — use hosted backend",
        "Fail fast when DB is down + add back button to role login screens",
        "Fix login instability and add guest login + auto-OTA",
        "Show a 'What's new' changelog in the in-app update popup",
        "Tenant portal Phase 1: role selection + tenant accounts"
      ],
      "releaseVersion": "1.0.0"
    }
  }
}